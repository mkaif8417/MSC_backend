require('dotenv').config(); const mongoose = require('mongoose'); mongoose.connect(process.env.MONGODB_URI).then(async () => { const indexes = await mongoose.connection.db.collection('studentattendances').indexes(); console.log(JSON.stringify(indexes, null, 2)); process.exit(0); });
